{
  "installCommand": "npm install --omit=optional --legacy-peer-deps",
  "buildCommand": "npm run build",
  "nodeVersion": "20.x",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}